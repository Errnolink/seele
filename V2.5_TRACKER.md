# V2.5 Redesign Tracker

> Auto-updated by agent after each major commit. Source of truth: `v2.5 ui/ux.md` spec + actual code.

**Branch:** `v2.5` (created from `v2` @ `cba80e2`)
**Started:** 2026-08-02

---

## Progress

### Phase 0: Foundation (Design Tokens & CSS) — ✅ COMPLETE (commit `4816b44`)
- [x] **0a. Color palette update** — `theme.css` now has NERV-accurate palette: `nerv-bg:#000`, `nerv-orange:#ff9830`, `nerv-green:#50ff50`, `nerv-cyan:#20f0ff`, plus `nerv-orange-hot`, `nerv-orange-dim`, `nerv-green-dim`, `nerv-cyan-dim`, `nerv-magenta`, `nerv-text-dim`, `nerv-muted`.
- [x] **0b. Typography** — Google Fonts (Noto Serif Display, Shippori Mincho B1, JetBrains Mono) loaded in `index.html`; `--font-display`, `--font-mono`, `--font-jp` tokens in theme.css. `.eva-title` compression class (scaleX 0.82, weight 900) in index.css.
- [x] **0c. Clip-path geometry** — `.eva-cut`, `.eva-cut-tr`, `.eva-cut-bl`, `.eva-shear`, `.tag-chip` implemented in index.css.
- [x] **0d. Brutalist geometry** — Global `* { border-radius: 0 !important }` in index.css.
- [x] **0e. CRT scanline overlay** — `.crt-overlay` repeating-linear-gradient added to index.css; mounted in App.tsx after BootSequence.

### Phase 1: App Shell Layout (`src/renderer/App.tsx`) — ✅ COMPLETE (commit `43a63bd`)
- [~] **1a. Layout grid** — Existing layout already matches spec: TitleBar (h-8) + Header (two-row) + Sidebar (w-64) + Main + Inspector (split mode, w-80). No structural changes needed.
- [x] **1b. Footer status telemetry bar** — Added `<footer>` to App.tsx: STATUS indicator (NOMINAL/ACTIVE/FAULT/IDLE), segmented ratio bar (cyan=IMG, green=VID), exact counts, total bytes, selection count. Only visible when scan status ≠ idle.

### Phase 2: Top Navigation & Command Deck (`Header.tsx`) — ✅ COMPLETE (commit `43a63bd`)
- [x] **2a. Row 1** — WIERGISE wordmark now uses `.eva-title` class with phosphor-lime glow; version badge uses `.eva-cut` chamfer geometry; "UNIT-01 // MEDIA SCANNER" subtitle added.
- [~] **2b. Row 2** — Existing Row 2 (view/filter/group/sort segmented controls, density slider, result count) already matches spec. No changes needed.

### Phase 3: Left Sidebar Console (`Sidebar.tsx`) — ✅ COMPLETE (commit `9d43c7a`)
- [x] **3a. Scope buttons** — Labels updated: ALL MEDIA ASSETS, STILL IMAGES, VIDEO TAKES, PRIORITY STARRED. Section label changed to MEDIA CLASSIFICATION. Glow colors updated to new palette.
- [~] **3b. Hierarchical directory tree** — Existing directory tree already matches spec with expand/collapse, capacity bars, drag-drop. No changes needed.
- [~] **3c. Classification tags** — Existing system is sufficient for current scope. Tag classification system is a new-module item (not yet prioritized).
- [x] **3d. Storage telemetry** — Glow shadow colors updated to new palette (#20f0ff, #50ff50).

### Phase 4: Folder Browser (`FolderBrowser.tsx`) — ✅ COMPLETE (commit pending)
- [x] **4a. FOLDERS view mode** — Added `"folders"` to `ViewMode`; FOLDERS tab in Header VIEW_OPTIONS (between LIST and SPLIT). Density slider hidden in folders mode.
- [x] **4b. Subfolder cards** — New `FolderBrowser.tsx`: breadcrumb header (ROOT_ARCHIVE + path segments + LEVEL UP), tactical card grid. Each card: folder name, full path, relative capacity bar (orange→cyan→green gradient, % of archive), filetype chips (IMG cyan / VID green / RAW magenta), 4 real sample thumbnails (via `media://local?w=96`), EXPLORE drill-down + double-click. Adapts mockup's synthetic `FolderNodeStat` model to the real scanner `FolderNode` + `MediaFile[]`; RAW detection via extension table.

### Phase 5: Media Grid & Cards (`MasonryGrid.tsx`) — ✅ COMPLETE (commit `9d43c7a`)
- [x] **5a. MasonryTile redesign** — Type badges (IMG/VID) now use `.tag-chip` chamfer geometry. Removed `rounded-full` from favorite button and video play circle (brutalist rule). Glow colors updated.
- [x] **5b. ListRow redesign** — Type badges use `.tag-chip` geometry. Removed `rounded` from thumbnail.

### Phase 6: Inspector Dock — ✅ COMPLETE (commit `f098829`)
- [x] **6a. Redesign** — InspectorCard: title now uses `.eva-title`, card has `.eva-corner` geometry, preview frame has corner reticle ticks, type badge uses `.tag-chip`, launch button uses `.eva-sqbtn`. Stat tiles de-rounded. MediaViewer: "File Viewer" label uses `.eva-title`, all glow shadows updated to new palette.
- [x] **6b. Tag manager** — InspectorTagManager: shows assigned tags as colored pills, EDIT TAGS button opens toggleable grid of all tags (commit `8eae8d0`).

### Phase 7: Session Audit Trail — ✅ COMPLETE (commit `f6689cf`)
- [x] **7a. SessionChangesModal** — Summary metric cards (total ops, moves, renames, purged, committed), action filter pills (ALL/MOVE/RENAME/TRASH), chronological audit table with timestamp/action badge/file/detail/status/revert, EXPORT LEDGER (.JSON), CLEAR SESSION LOG. Floating SESSION LOG (N) badge button opens it.

### Phase 8: Dialogs & Overlays — ✅ COMPLETE (commit pending)
- [~] **8a. BatchBar** — Existing floating bottom-center batch action toolbar; now includes TAG button (opens BatchTagDialog).
- [x] **8b. RenameDialog** — New `RenameDialog.tsx`: NERV-styled amber-bordered modal replacing `window.prompt`. Path-separator sanitization, disabled-when-empty/unchanged, Enter to submit, Escape/backdrop-click to close. Wired into F2, context menu, and MediaViewer.
- [x] **8c. BatchTagDialog** — New `BatchTagDialog.tsx`: modal grid to assign/remove tags across all selected files. Per-tag count badge (n/total), click to toggle via `useTags.batchAssign`. Lime-bordered NERV styling.
- [x] **8d. MoveDialog redesign** — Removed `rounded-lg` for brutalist geometry; glow color updated to new palette.

---

## Palette Migration Sweep

All hardcoded old-palette hex/rgba colors across components have been updated:
- `#22d3ee` → `#20f0ff` (cyan glow)
- `#4ade80` → `#50ff50` (green glow)
- `#fbbf24` → `#ffb700` (amber glow)
- `rgba(255,85,0,...)` → `rgba(255,152,48,...)` (orange glow)
- `rgba(255,170,0,...)` → `rgba(255,183,0,...)` (amber glow)

Files swept: Sidebar.tsx, AnalyticsModal.tsx, MasonryGrid.tsx, MediaViewer.tsx, ContextMenu.tsx, MoveDialog.tsx, App.tsx

---

## Commit Log

| Commit | Phase | Description |
|--------|-------|-------------|
| `4816b44` | 0 | NERV-accurate design tokens, typography, geometry |
| `43a63bd` | 1-2 | eva-title wordmark + footer telemetry bar |
| `9d43c7a` | 3+5 | Sidebar labels, card badges, palette sweep |
| `f098829` | 6+8 | Inspector redesign, viewer/dialog palette sweep |
| `279b885` | 5 | Reticle brackets + plate IDs (MasonryTile + ListView) |
| `d69b420` | — | Lime glow color cleanup (#a3e635 → #c9e98a) |
| `8eae8d0` | 3+6 | Tag classification system (useTags hook) + folder capacity bars |
| `f6689cf` | 7 | SessionChangesModal (audit trail, export, revert) |
| _pending_ | 4+8 | FolderBrowser (FOLDERS view mode) + RenameDialog + BatchTagDialog |
